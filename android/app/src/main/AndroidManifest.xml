<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <!-- HTTP se permite solo para el alias local 10.0.2.2 mediante network_security_config.xml.
         Las demás conexiones conservan la política segura predeterminada de Android. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Solo para el recordatorio opt-in de rutinas (services/device/routineReminders.ts).
         Android 13+ (API 33+) exige este permiso en tiempo de ejecución para local-notifications;
         se solicita en el momento en que la persona usuaria activa "Recordarme esta rutina",
         nunca al iniciar la app — ver spec/features/011-capacidades-dispositivo/. No se declara
         SCHEDULE_EXACT_ALARM ni USE_EXACT_ALARM: el recordatorio no es crítico al minuto. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- El plugin declara este permiso por defecto, pero la app agenda recordatorios inexactos. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
</manifest>
